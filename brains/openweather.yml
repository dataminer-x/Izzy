  - name: "weather-query"
    signals:
      - order: "what's the weather"
    neurons:
      - uri:
          url: "https://api.openweathermap.org/data/2.5/onecall?lat={{ lat }}&lon={{ lon }}&units=imperial&appid={{ wmapAPI }}"
          say_template:
            - "The current temperature is {{ temp }} and the current condition is {{ description }}"